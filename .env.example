# Dev local (SQLite) — zéro installation
# Chemin résolu relatif à prisma/schema.prisma → crée prisma/dev.db
DATABASE_URL="file:./dev.db"

# Prod (Vercel) — remplacer par du PostgreSQL hébergé (Neon/Supabase)
# DATABASE_URL="postgresql://user:password@host:5432/sabrenshop?schema=public"

AUTH_SECRET="génère-avec-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# Vercel Blob (prod). Laisser vide en dev (stockage local /public/uploads)
BLOB_READ_WRITE_TOKEN=""

NEXT_PUBLIC_WHATSAPP_NUMBER="22789148454"